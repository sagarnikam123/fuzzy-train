apiVersion: apps/v1
kind: Deployment
metadata:
  name: fuzzy-train-stdout
  labels:
    app: fuzzy-train
    output: stdout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fuzzy-train
      output: stdout
  template:
    metadata:
      labels:
        app: fuzzy-train
        output: stdout
    spec:
      containers:
      - name: fuzzy-train
        image: sagarnikam123/fuzzy-train:2.1.1
        args:
          - "--min-log-length"
          - "90"
          - "--max-log-length"
          - "150"
          - "--lines-per-second"
          - "1"
          - "--time-zone"
          - "UTC"
          - "--log-format"
          - "JSON"
          - "--output"
          - "stdout"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      restartPolicy: Always