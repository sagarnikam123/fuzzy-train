apiVersion: apps/v1
kind: Deployment
metadata:
  name: fuzzy-train-file
  labels:
    app: fuzzy-train
    output: file
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fuzzy-train
      output: file
  template:
    metadata:
      labels:
        app: fuzzy-train
        output: file
    spec:
      containers:
      - name: fuzzy-train
        image: sagarnikam123/fuzzy-train:2.1.1
        args:
          - "--min-log-length"
          - "100"
          - "--max-log-length"
          - "200"
          - "--lines-per-second"
          - "2"
          - "--time-zone"
          - "UTC"
          - "--log-format"
          - "JSON"
          - "--output"
          - "file"
          - "--file"
          - "/logs/"
        volumeMounts:
        - name: log-volume
          mountPath: /logs
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      volumes:
      - name: log-volume
        emptyDir: {}
      restartPolicy: Always
